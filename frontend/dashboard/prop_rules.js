let propCatalog={};async function PR(p,o={}){let r=await fetch(p,{credentials:"same-origin",cache:"no-store",...o,headers:{"Content-Type":"application/json"}}),d=await r.json().catch(()=>({}));if(!r.ok)throw Error(d.error||"request_failed");return d}function O(e,v){e.innerHTML=v.map(x=>`<option>${x}</option>`).join("")}function programs(){let f=propFirm.value;O(propProgram,Object.keys(propCatalog[f]||{}));phases()}function phases(){O(propPhase,Object.keys(propCatalog[propFirm.value]?.[propProgram.value]||{}));sizes()}function sizes(){O(propAccountSize,propCatalog[propFirm.value]?.[propProgram.value]?.[propPhase.value]||[])}function mode(){propControls.hidden=accountMode.value!=="PROP_FIRM"}async function load(){let d=await PR("/api/prop-rules/catalog");propCatalog=d.catalog;O(propFirm,Object.keys(propCatalog));programs();mode()}accountMode?.addEventListener("change",mode);propFirm?.addEventListener("change",programs);propProgram?.addEventListener("change",phases);propPhase?.addEventListener("change",sizes);applyPropRules?.addEventListener("click",async()=>{let b={mode:accountMode.value};if(b.mode==="PROP_FIRM")Object.assign(b,{firm:propFirm.value,program:propProgram.value,phase:propPhase.value,account_size:Number(propAccountSize.value)});try{let d=await PR("/api/prop-rules/select",{method:"POST",body:JSON.stringify(b)});propStatus.textContent=d.policy_status;propRulesPreview.textContent=JSON.stringify(d.rules||{},null,2)}catch(e){propStatus.textContent=e.message}});load();
